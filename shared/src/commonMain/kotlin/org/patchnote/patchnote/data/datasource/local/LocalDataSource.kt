package org.patchnote.patchnote.data.datasource.local

/**
 * TODO: [Local DataSource] 기기 내부 저장소 데이터 제어
 * 1. 인터페이스에 로컬 DB(SqlDelight/Room)나 Preference 접근 메서드 정의
 * 2. Fake 구현체에서 앱 종료 후에도 유지되어야 할 가짜 데이터 관리
 * 3. 캐싱 전략(Offline-first)을 위한 기초 데이터 공급원으로 활용
 */

class LocalDataSource {
}