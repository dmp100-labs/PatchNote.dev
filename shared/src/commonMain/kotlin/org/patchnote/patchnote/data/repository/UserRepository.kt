package org.patchnote.patchnote.data.repository

class UserRepository {
    /**
     * TODO: [5단계 - Repository] 데이터 흐름 통합 및 전달
     * 1. DataSource로부터 DTO를 받아오고, Mapper를 사용하여 UI 모델로 변환
     * 2. ViewModel이 가짜(Fake)인지 진짜(Real)인지 모르게 인터페이스(UserRepository) 뒤로 숨김
     * 3. (추후) 로컬 DB(SqlDelight)가 필요해지면 여기서 Remote와 Local 데이터를 중재
     */
}