package org.patchnote.patchnote.data.repository

/**
 * TODO: [Repository] 로컬과 리모트 데이터 통합 및 중재
 * 1. 로컬에 데이터가 있으면 로컬에서, 없으면 리모트에서 가져오는 'Single Source of Truth' 전략 수립
 * 2. RemoteDataSource에서 받은 DTO를 Local DB에 저장하는 캐싱 로직 구현
 * 3. 최종적으로 UI 모델(KidWishUiModel)로 변환하여 ViewModel에 전달
 */

class UserRepositoryImpl {
}