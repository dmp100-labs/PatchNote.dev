package org.patchnote.patchnote.data.mapper

class UserMapper {
}
/**
 * TODO: [3단계 - Mapper] 서버 데이터(DTO)를 UI 모델로 변환
 * 1. UserDto.toUiModel(): UserUiModel 확장 함수 구현
 * 2. 서버의 Raw 데이터를 화면에 표시하기 좋은 형태(날짜 포맷, 문자열 결합 등)로 가공
 * 3. 서버 응답이 비어있을 경우를 대비한 기본값(Default Value) 처리
 */